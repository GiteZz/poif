from pathlib import Path
from poif.versioning.dataset import ResourceDirCollection
from poif.dataset.base import MultiDataset


def get_resource_dir():
    current_loc = Path(__file__)
    ds_dir = current_loc.parent
    return ds_dir / "resources"


versioned_collection = ResourceDirCollection(get_resource_dir())
{{data.dataset_name}}_files = versioned_collection.get_files()


{{data.dataset_name}} = MultiDataset()
{{data.dataset_name}}.form({{data.dataset_name}}_files)
