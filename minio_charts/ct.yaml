# See https://github.com/helm/chart-testing#configuration
remote: origin
chart-dirs:
  - minio_charts
chart-repos:
  - incubator=https://kubernetes-minio_charts-incubator.storage.googleapis.com/
helm-extra-args: --timeout=500s
